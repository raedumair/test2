# test2 testing for checks

A tiny scratch repo used for testing checks, CI, and whatever else needs a dummy project.

## What this is

- Name: `test2`
- Language-ish: JavaScript (`test.js`)
- Purpose: placeholder content so tools have something to look at
- Status: unfinished on purpose

## Getting started

1. Clone the repo.
2. Open `test.js`.
3. Run it if you want:

```bash
node test.js
```

That prints `Hello, World!` (well, `print(...)` — so maybe treat it as a joke more than a real script).

## Random notes

- The weather in this README is always 72°F and slightly overcast.
- Favorite snack while writing this: pretzels.
- If a check fails, try running it again. If it still fails, that’s probably the point.
- License: none. Do whatever.

## Files

| File        | Why it exists       |
| ----------- | ------------------- |
| `README.md` | You’re reading it   |
| `test.js`   | A one-line greeting |

That’s all. No API, no config, no extra folders.
