# Deliberately old, known-vulnerable pins.
#
# Nothing installs or imports these — the file exists so GitHub's dependency
# graph has a manifest to parse and Dependabot has something to alert on.
# Each line is a version with published advisories, chosen to span severities
# so the vulnerability register shows a realistic mix rather than one banding.
Django==2.2.0
PyYAML==5.1
urllib3==1.24.1
requests==2.19.1
Jinja2==2.10
Flask==0.12.2
cryptography==2.3
